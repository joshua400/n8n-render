import { Request, Response } from 'express';
import { LoginRequest, UserRequest } from '../requests';
import type { PublicUser } from '../Interfaces';
import { PostHogClient } from '../posthog';
import { InternalHooks } from '../InternalHooks';
import { License } from '../License';
import { UserService } from '../services/user.service';
import { MfaService } from '../Mfa/mfa.service';
import { Logger } from '../Logger';
export declare class AuthController {
    private readonly logger;
    private readonly internalHooks;
    private readonly mfaService;
    private readonly userService;
    private readonly license;
    private readonly postHog?;
    constructor(logger: Logger, internalHooks: InternalHooks, mfaService: MfaService, userService: UserService, license: License, postHog?: PostHogClient | undefined);
    login(req: LoginRequest, res: Response): Promise<PublicUser | undefined>;
    currentUser(req: Request, res: Response): Promise<PublicUser>;
    resolveSignupToken(req: UserRequest.ResolveSignUp): Promise<{
        inviter: {
            firstName: string;
            lastName: string;
        };
    }>;
    logout(req: Request, res: Response): {
        loggedOut: boolean;
    };
    private validateMfaToken;
    private validateMfaRecoveryCode;
}
