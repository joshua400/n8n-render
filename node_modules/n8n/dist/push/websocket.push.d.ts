import type WebSocket from 'ws';
import { Logger } from '../Logger';
import { AbstractPush } from './abstract.push';
import type { User } from '../databases/entities/User';
export declare class WebSocketPush extends AbstractPush<WebSocket> {
    constructor(logger: Logger);
    add(sessionId: string, userId: User['id'], connection: WebSocket): void;
    protected close(connection: WebSocket): void;
    protected sendToOne(connection: WebSocket, data: string): void;
    private pingAll;
}
